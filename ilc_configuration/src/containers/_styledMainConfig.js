import styled from 'styled-components'
import { AppBar as appBar, Toolbar, IconButton, Button, Typography } from '@material-ui/core';

export const FormWrapper = styled.div`
    margin-top: 0rem;
    margin-left: 0rem;
    height: calc(100vh - 100px);
    overflow-y: auto;
    overflow-x: hidden;
`